<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid title-navbar">
    <a class="navbar-brand text-warning" href="{{path('home')}}" style="font-size: 30px;">EVER NATURALS</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active text-warning" aria-current="page" href="#advices">Nos Conseils</a>
        {# <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-warning" href="#advices" id="navbarDropdownMenuLink" role="button"
            data-bs-toggle="dropdown" aria-expanded="false">
            Nos conseils
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            {% for article in articles %}
            <li><a class="dropdown-item text-warning title-navbar" href="{{path ('article_show', {'slug': article.slug})}}"></a>{{ article.title|u.truncate(30, '...') }}</a></li>
            {% endfor %}
          </ul> #}
        </li>
        <li class="nav-item">
          <a class="nav-link active text-warning" aria-current="page" href="{{path('products')}}">Nos produits</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-warning" href="#">Nous contacter</a>
        </li>
        {% if is_granted('ROLE_BUYER') %}
        <li class="nav-item">
          {% if app.session.get('panier') %}
          <a class="nav-link text-warning" href="{{path('cart_index')}}">
            <i class="fab fa-opencart"></i>
            <span class="badge badge-danger color-badge">{{ app.session.get('count') }}</span>
          </a>
          {% else %}
          <a class="nav-link text-warning" href="{{path('cart_index')}}">
            <i class="fab fa-opencart"></i>
          </a>
          {% endif %}
        </li>
        <li class="nav-item dropdown ">
          <a class="nav-item dropdown-toggle btn btn-light text-center nav-link-button" id="dropdown1"
            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="far fa-user"></i>
          </a>
          <div class="dropdown-menu dropdown-menu-right text-center">
            <a href="" class="my-account">Mon compte</a>
            <br>
            {% if is_granted('ROLE_BUYER') %}
            <a href="" class="my-account">Mes commandes</a>
            {% endif %}
            <br>
            <a href="{{path('app_logout')}}" class="my-account">
              <i class="fas fa-sign-out-alt"></i>
              Se déconnecter</a>
          </div>
        </li>
        {% elseif is_granted('ROLE_ADMIN') %}
        <a href="{{path('admin')}}" class="btn btn-light text-center nav-item nav-link-button mr-1">
          <i class="far fa-user"></i>
          Administration</a>
        <a href="{{path('app_logout')}}" class="nav-link text-warning">
          <i class="fas fa-sign-out-alt"></i>
        </a>
        {% else %}
        <a href="{{path('app_login')}}" class="btn btn-light text-center nav-item nav-link-button mr-1">Connectez-vous</a>
        <a href="{{path('app_register')}}" class="btn btn-light text-center nav-item nav-link-button mobile-nav-margin">Inscrivez-vous</a>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>